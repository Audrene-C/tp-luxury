{% extends 'base.html.twig' %}

{% block title %}JobOffer{% endblock %}

{% block body %}
<div class="base">

    <!-- Page -->
    <div class="page">

        <!-- Page Header-->
        <section class="page-title page-title-bg fixed-bg overlay dark-5 padding-top-160 padding-bottom-80">
            <div class="container">
                <div class="row">
                    <div class="col-md-12">
                        <h2 class="white-text">Show Job Offer</h2>
                        <span class="white-text">Welcome on board</span>
                        <ol class="breadcrumb">
                            <li><a href="#!">Home</a></li>
                            <li class="active">Jobs Offers</li>
                        </ol>
                    </div>
                </div>
            </div>
        </section>


        <!-- Page Content-->
        <section class="section-padding gray-bg log-section">
            <div class="container">
                <div class="row">

                    <table class="table">
                        <tbody>
                            <tr>
                                <th>Id</th>
                                <td>{{ job_offer.id }}</td>
                            </tr>
                            <tr>
                                <th>Description</th>
                                <td>{{ job_offer.description }}</td>
                            </tr>
                            <tr>
                                <th>Active</th>
                                <td>{{ job_offer.active ? 'Yes' : 'No' }}</td>
                            </tr>
                            <tr>
                                <th>Notes</th>
                                <td>{{ job_offer.notes }}</td>
                            </tr>
                            <tr>
                                <th>JobTitle</th>
                                <td>{{ job_offer.jobTitle }}</td>
                            </tr>
                            <tr>
                                <th>ContractType</th>
                                <td>{{ job_offer.contractType }}</td>
                            </tr>
                            <tr>
                                <th>Location</th>
                                <td>{{ job_offer.location }}</td>
                            </tr>
                            <tr>
                                <th>ClosingDate</th>
                                <td>{{ job_offer.closingDate ? job_offer.closingDate|date('Y-m-d') : '' }}</td>
                            </tr>
                            <tr>
                                <th>Salary</th>
                                <td>{{ job_offer.salary }}</td>
                            </tr>
                            <tr>
                                <th>CreatedAt</th>
                                <td>{{ job_offer.createdAt ? job_offer.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                            </tr>
                        </tbody>
                    </table>

                    <a href="{{ path('job_offer') }}" class="btn btn-md primary border waves-effect waves-dark">back to list</a>

                    <a href="{{ path('job_offer_edit', {'id': job_offer.id}) }}" class="btn btn-md primary waves-effect waves-light">edit</a>

                    {{ include('job_offer/_delete_form.html.twig') }}

                    

                </div>
            </div>
        </section>

    </div>

</div>

    
{% endblock %}
